# infrastructure/mcp/servers/chrome-devtools/tools/ — 模块记忆

## 职责定位
Chrome DevTools MCP 服务的工具参数声明（JSON Schema）。

## 文件
- `screenshot.json`：页面截图。
- `click.json`：元素点击。
- `get_content.json`：页面内容提取。

## 业务边界要点
- 仅为声明文件，无内置 adapter，当前不可实际执行。
